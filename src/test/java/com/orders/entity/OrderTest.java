/*
package com.orders.entity;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class OrderTest {

    @Test
    void testGettersAndSetters() {
        // Arrange
*/
/*        Order order = new Order();
        Long id = 1L;
        double distance = 10.5;
        boolean taken = true;
        String status = "DELIVERED";
        String origin = "123 Main St";
        String destination = "456 Park Ave";
        Long orderId = 100L;
        String[] originCoordinates = {"1.234,5.678"};
        String[] destinationCoordinates = {"9.876,5.432"};

        // Act
        order.setId(id);
        order.setDistance(distance);
        order.setTaken(taken);
        order.setStatus(status);
        order.setOrigin(origin);
        order.setDestination(destination);
        order.setOrderId(orderId);
        order.setOriginCoordinates(originCoordinates);
        order.setDestinationCoordinates(destinationCoordinates);

        // Assert
        assertEquals(id, order.getId());
        assertEquals(distance, order.getDistance());
        assertEquals(taken, order.isTaken());
        assertEquals(status, order.getStatus());
        assertEquals(origin, order.getOrigin());
        assertEquals(destination, order.getDestination());
        assertEquals(orderId, order.getOrderId());
        assertArrayEquals(originCoordinates, order.getOriginCoordinates());
        assertArrayEquals(destinationCoordinates, order.getDestinationCoordinates());
    }*//*

}
*/
